# Claude code data analytics skills 

The purpose of this codebase is to collect and manage the claude code skills I will use daily, I am a data researcher. 
I use python, SQL (lite, oracle, elastic, postgres) and S3. 
I work with many different data types, including the regular (tabular, log, time series, JSON)
    but also raw bits I need to parse in specific formats)
I prefer to use pandas / polars depending on data size.
I always use plotly for my visualizations.

Sometimes I use old data (python 2/matlab/python3) that is very poorly written.